const fs = require("fs");
const path = require("path");

const file = path.join(__dirname, "../frontend/assets/js/language.js");
let text = fs.readFileSync(file, "utf8");

// EN
text = text.replace(
  'filter_special: "Special",',
  'filter_special: "Special", view_all_specials: "View all specials",'
);

// TE
text = text.replace(
  'filter_special: "?????????????",',
  'filter_special: "?????????????", view_all_specials: "????? ???????? ?????? ??????",'
);

// HI
text = text.replace(
  'filter_special: "?????",',
  'filter_special: "?????", view_all_specials: "??? ????? ?????? ?????",'
);

fs.writeFileSync(file, text, "utf8");
console.log("Fixed language.js translations properly!");
